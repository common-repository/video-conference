<?php

/**
 *
 * @since      1.0.3
 * @package    Video_Conference
 * @subpackage Video_Conference/includes
 * @author     Helmi <pm2monit@gmail.com>
 */
class Video_Conference_Activator {

	public static function activate() {

		// global $wpdb;
		// $table_name = $wpdb->prefix . 'video_conference';
		// $charset_collate = $wpdb->get_charset_collate();
		// $sql = "CREATE TABLE $table_name (
		// 	id mediumint(11) NOT NULL AUTO_INCREMENT,
		// 	iduser_wp varchar(100) NULL,
		// 	idmeeting varchar(100) NULL,
		// 	nama_meting varchar(100) NULL,
		// 	token text NULL,
		// 	role_moderator ENUM('0','1') DEFAULT '0' NOT NULL,
		// 	status_open ENUM('0','1') DEFAULT '0' NOT NULL,
		// 	date_created datetime DEFAULT CURRENT_TIMESTAMP NULL,
		// 	date_update varchar(255) NULL,
		// 	dalete_ ENUM('0','1') DEFAULT '0' NOT NULL,
		// 	created_id varchar(255) DEFAULT '' NULL,
		// 	PRIMARY KEY  (id)
		// ) $charset_collate;";

		// require_once( ABSPATH . 'wp-admin/includes/upgrade.php' );
		// dbDelta( $sql );

	}

}
